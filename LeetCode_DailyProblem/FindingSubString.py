s = "abcabcabcabc"

def subString(s,l):
    '''we need to find the substrings of the given string'''
    # we will have to traverse the length of the string to get the substring
    for i in range (1,n+1):

subString(Str,len(Str))